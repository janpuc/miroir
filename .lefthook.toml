[[remotes]]
git_url = "https://github.com/home-operations/.github"
ref = "main"
configs = ["lefthook.common.toml"]

# Regenerate the chart's committed README + values.schema.json whenever their inputs
# change, and stage them. CI's helm-docs-check is the backstop.
[pre-commit.commands.helm-docs]
glob = [
  "charts/miroir/Chart.yaml",
  "charts/miroir/values.yaml",
  "charts/miroir/README.md.gotmpl",
]
run = "mise run helm-docs && git add charts/miroir/README.md charts/miroir/values.schema.json"

# The generated chart README is .prettierignore'd (helm-docs owns it); keep it out
# of the shared markdown formatter's file list too, or a commit staging only it
# hands oxfmt an all-ignored set and it exits non-zero. CHANGELOG.md is
# .prettierignore'd for the same reason (release-please owns it), and a release
# bump stages it with no other markdown — the all-ignored case exactly.
[pre-commit.commands.format-markdown]
exclude = ["charts/miroir/README.md", "CHANGELOG.md"]

# Likewise .release-please-manifest.json: .prettierignore'd, and the only JSON a
# release bump stages.
[pre-commit.commands.format-json]
exclude = ["charts/miroir/values.schema.json", ".release-please-manifest.json"]

# Generated CRD/RBAC YAMLs are .prettierignore'd; exclude them here too, or a
# commit staging only generated YAML feeds oxfmt an all-ignored input list and
# it exits 2. (An earlier `run = "... || true"` override stopped working when
# lefthook v2 dropped shell interpretation of run strings.)
[pre-commit.commands.format-yaml]
exclude = [
  "charts/miroir/crds/*.yaml",
  "config/crd/bases/*.yaml",
  "config/rbac/role.yaml",
  "config/webhook/manifests.yaml",
]
