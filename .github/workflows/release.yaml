---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Release

on:
  push:
    branches:
      - main
    paths-ignore:
      - "**.md"
  release:
    types:
      - published
  # Dispatched at a release tag this publishes exactly what `release:
  # published` publishes — same ref, same semver metadata. It is the only
  # way a fork reaches this workflow at all (a release cut with
  # GITHUB_TOKEN cannot raise the release event), and it doubles as the
  # manual re-publish button. Everything below therefore keys off "not a
  # branch push" rather than the release event specifically.
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'push' }}

jobs:
  versions:
    name: Versions
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    outputs:
      go-version: ${{ steps.versions.outputs.go-version }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Mise
        uses: jdx/mise-action@3c2e0cf82a5b2e5249f0d3635a4d83d0ae861518 # v4.2.5
        with:
          cache: false
          experimental: true
          install: false

      - name: Resolve versions
        id: versions
        run: echo "go-version=$(mise config get tools.go)" >> "$GITHUB_OUTPUT"

  # Two images from one Dockerfile (#110): the controller ships distroless
  # (it never execs a storage CLI), the agent ships the Debian storage
  # userland. Separate jobs keep each builder's first-class `digest`
  # output addressable for the pin step (kopiur's layout).
  release-controller:
    name: Release (controller)
    needs: versions
    permissions:
      contents: read
      id-token: write
      packages: write
    uses: docker/github-builder/.github/workflows/build.yml@58cb9f5b71b1836d6f690c1e95effdeb9b98cb8a # v1.17.0
    with:
      build-args: |
        VERSION=${{ github.ref_name }}
        REVISION=${{ github.sha }}
        GO_VERSION=${{ needs.versions.outputs.go-version }}
      cache: true
      cache-scope: controller
      target: controller
      meta-images: ghcr.io/${{ github.repository }}-controller
      meta-tags: |
        type=semver,pattern={{version}}
        type=raw,value=rolling,enable=${{ github.event_name != 'push' }}
        type=ref,event=branch
      output: image
      platforms: linux/amd64,linux/arm64
      push: true
      sbom: true
      set-meta-annotations: true
      set-meta-labels: true
      sign: true
    secrets:
      registry-auths: |
        - registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

  release-agent:
    name: Release (agent)
    needs: versions
    permissions:
      contents: read
      id-token: write
      packages: write
    uses: docker/github-builder/.github/workflows/build.yml@58cb9f5b71b1836d6f690c1e95effdeb9b98cb8a # v1.17.0
    with:
      build-args: |
        VERSION=${{ github.ref_name }}
        REVISION=${{ github.sha }}
        GO_VERSION=${{ needs.versions.outputs.go-version }}
      cache: true
      cache-scope: agent
      target: agent
      meta-images: ghcr.io/${{ github.repository }}-agent
      meta-tags: |
        type=semver,pattern={{version}}
        type=raw,value=rolling,enable=${{ github.event_name != 'push' }}
        type=ref,event=branch
      output: image
      platforms: linux/amd64,linux/arm64
      push: true
      sbom: true
      set-meta-annotations: true
      set-meta-labels: true
      sign: true
    secrets:
      registry-auths: |
        - registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

  # Gateway: the agent userland plus NFS-Ganesha, spawned per RWX volume to
  # export it over NFS. Same builder layout as the agent so its digest is
  # addressable for the pin step.
  release-gateway:
    name: Release (gateway)
    needs: versions
    permissions:
      contents: read
      id-token: write
      packages: write
    uses: docker/github-builder/.github/workflows/build.yml@58cb9f5b71b1836d6f690c1e95effdeb9b98cb8a # v1.17.0
    with:
      build-args: |
        VERSION=${{ github.ref_name }}
        REVISION=${{ github.sha }}
        GO_VERSION=${{ needs.versions.outputs.go-version }}
      cache: true
      cache-scope: gateway
      target: gateway
      meta-images: ghcr.io/${{ github.repository }}-gateway
      meta-tags: |
        type=semver,pattern={{version}}
        type=raw,value=rolling,enable=${{ github.event_name != 'push' }}
        type=ref,event=branch
      output: image
      platforms: linux/amd64,linux/arm64
      push: true
      sbom: true
      set-meta-annotations: true
      set-meta-labels: true
      sign: true
    secrets:
      registry-auths: |
        - registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

  helm:
    name: Helm
    needs: [release-controller, release-agent, release-gateway]
    if: ${{ github.event_name != 'push' }}
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      id-token: write
      packages: write
    env:
      # github.ref is the tag ref for a release event, and the dispatch
      # above is documented to be aimed at one, so both paths agree — and
      # agree with the VERSION build-arg the images were stamped with.
      VERSION: ${{ github.ref_name }}
    steps:
      # Fail legibly rather than handing helm a branch name to package as a
      # semver version.
      - name: Require a tag ref
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        run: |
          echo "::error::dispatch this workflow at a release tag: gh workflow run release.yaml --ref <tag>"
          exit 1

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Mise
        uses: jdx/mise-action@3c2e0cf82a5b2e5249f0d3635a4d83d0ae861518 # v4.2.5
        with:
          cache: false
          experimental: true
          install_args: --locked

      - name: Generate manifests and CRDs
        run: mise run helm-crds

      # A chart published from this repository has to install this
      # repository's images: a fork's release would otherwise ship
      # upstream's, quietly, and nothing built here would ever run. These
      # are the same names the builder jobs above pushed to. GHCR paths are
      # lowercase, which an owner or repo name need not be.
      - name: Point the chart at this repository's images
        env:
          IMAGE_BASE: ghcr.io/${{ github.repository }}
          CHART_BASE: ghcr.io/${{ github.repository_owner }}/charts
        run: |
          images="$(echo "${IMAGE_BASE}" | tr '[:upper:]' '[:lower:]')"
          echo "CHART_REPO=$(echo "${CHART_BASE}" | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"
          yq -i ".image.repository = \"${images}-controller\"" charts/miroir/values.yaml
          yq -i ".agent.image.repository = \"${images}-agent\"" charts/miroir/values.yaml
          yq -i ".gateway.image.repository = \"${images}-gateway\"" charts/miroir/values.yaml

      # Pin every component image to the exact digest its builder produced
      # (the signed multi-arch indexes) so installs are immutable. Fail
      # closed if any digest is missing.
      - name: Pin the released image digests into the chart
        env:
          CONTROLLER_DIGEST: ${{ needs.release-controller.outputs.digest }}
          AGENT_DIGEST: ${{ needs.release-agent.outputs.digest }}
          GATEWAY_DIGEST: ${{ needs.release-gateway.outputs.digest }}
        run: |
          for pair in "controller:${CONTROLLER_DIGEST}" "agent:${AGENT_DIGEST}" "gateway:${GATEWAY_DIGEST}"; do
            comp="${pair%%:*}"
            digest="${pair#*:}"
            case "${digest}" in
              sha256:*) ;;
              *) echo "::error::build produced no digest for ${comp} (got: '${digest}')"; exit 1 ;;
            esac
            case "${comp}" in
              controller) path=".image.digest" ;;
              agent)      path=".agent.image.digest" ;;
              gateway)    path=".gateway.image.digest" ;;
            esac
            yq -i "${path} = \"${digest}\"" charts/miroir/values.yaml
            echo "pinned ${comp} -> ${digest}"
          done

      - name: Package Helm charts
        run: |
          helm package charts/miroir --version "${VERSION}" --app-version "${VERSION}"

      - name: Log in to Container Registry
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish Helm charts to GHCR (OCI)
        run: |
          helm push "miroir-${VERSION}.tgz" "oci://${CHART_REPO}"

      - name: Sign the published charts with Cosign
        run: |
          cosign sign --yes "${CHART_REPO}/miroir:${VERSION}"
